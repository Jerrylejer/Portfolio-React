import React from 'react';
import Header from '../../components/Header/Header';

const Viepro = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default React.memo(Viepro);