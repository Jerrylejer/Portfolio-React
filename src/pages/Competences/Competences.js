import React from 'react';
import Header from '../../components/Header/Header';

const Competences = () => {
    return (
        <div className="competences">
        <Header />
            
        </div>
    );
};

export default React.memo(Competences);